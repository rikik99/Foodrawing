<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.food.domain.order.mapper.ReserveMapper">
  
  <select id="getReservesByCustomerId">
  	SELECT 
            COALESCE(SUM(
                CASE 
                    WHEN PLUSMINUS = '+' THEN RESERVES 
                    WHEN PLUSMINUS = '-' THEN -RESERVES 
                    ELSE 0 
                END
            ), 0) AS totalReserves
        FROM 
            CUSTOMER_RESERVES_TB
        WHERE 
            CUSTOMER_ID = #{customerId}
  </select>  
	
</mapper>
